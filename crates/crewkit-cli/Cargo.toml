[package]
name = "crewkit-cli"
description = "Crewkit command line: install, inspect and remove AI kits from the terminal, sharing the desktop app's engine."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[[bin]]
name = "crewkit"
path = "src/main.rs"

[dependencies]
crewkit-core = { path = "../crewkit-core" }
