{
  "id": "claude-desktop",
  "name": "Claude Desktop",
  "appPaths": ["/Applications/Claude.app"],
  "files": {
    "config": "${appSupport}/Claude/claude_desktop_config.json"
  },
  "restartRequired": true,
  "notes": "Local config accepts only stdio MCP servers, so remote servers are installed as crewkit-bridge entries. Restart the app to pick up changes."
}
