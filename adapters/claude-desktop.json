{
  "id": "claude-desktop",
  "name": "Claude Desktop",
  "appPaths": [
    "/Applications/Claude.app",
    "${home}/Applications/Claude.app",
    "${localAppData}/AnthropicClaude",
    "${localAppData}/Packages/Claude_pzs8sxrjxfjjc"
  ],
  "files": {
    "config": "${appSupport}/Claude/claude_desktop_config.json",
    "configStore": "${localAppData}/Packages/Claude_pzs8sxrjxfjjc/LocalCache/Roaming/Claude/claude_desktop_config.json"
  },
  "restartRequired": true,
  "notes": "Local config accepts only stdio MCP servers, so remote servers are installed as crewkit-bridge entries. Restart the app to pick up changes. The Microsoft Store (MSIX) build lives under its package-family dir and reads the config from its virtualized LocalCache, not the real %APPDATA% — Paths::claude_desktop_config picks the right one."
}
